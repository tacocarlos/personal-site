"use client";

import { Polynomial } from "./polynomial";

export default function page() {
    const f = Polynomial.fromString("");
    // alert(f.toString());
    return (
        <div>
            <p>
                Check console
            </p>
        </div>
    )
}